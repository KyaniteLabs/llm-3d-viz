# Contributing

1. Open an issue or claim a `ready-for-agent` ticket.
2. Branch from `main`; keep PRs focused.
3. `npm test` and `npm run build` must pass.
4. Prefer extending `pointEncoding` / `src/config` over one-off stage hacks.
5. No secrets, private hostnames, or deploy tokens in commits.

# Context snapshot — v0 build, llm-3d-viz

Captured: 2026-08-01T21:31:09Z (UTC). Prepared for `$ralplan` (consensus, non-interactive).

## Task statement

Build v0 of llm-3d-viz per SPEC §8: curated dataset + 3D hero scatter (de-chromed Plotly) + linked 2D projections + tunable value-score + threshold-sweep + cinematic mode, styled to DESIGN-SYSTEM.md (Observatory-after-dark). Output: a static `dist/` ready for (approval-gated) publish to Cloudflare Pages, and enough visual quality to start recording videos (D6).

## Desired outcome

`vite build` produces a static bundle that renders the full v0 experience with zero default-Plotly chrome, passes unit + render verification, and clears `tastecheck-pass` (resolving wayfinder ticket #10).

## Known facts / evidence

- SPEC.md: D1–D8 locked — hybrid encoding (D1), all three uses (D2), curated static data (D3), all differentiators phased (D4), full app phased (D5), aesthetic first-class (D6), Plotly v0 (D7), tastecheck-driven design (D8). Never re-litigate.
- DESIGN-SYSTEM.md: approved tokens, motion curves, refusals, build order. Filament-white `#E8F1E4` on ink `#070C0B`; threshold-sweep signature; Plotly = render engine only; points by shape not color; ridge not surface; no ambient motion; reduced-motion path required.
- Wayfinder map #2 (Forgejo) — 7 of 8 tickets resolved:
  - #3 dataset: `data/models.v0.draft.json`, 35 models, 2026-08-01 snapshot; anomalies: reasoner TTFT 74–188s; two $0.00-price models; arena_elo/swe_bench null everywhere.
  - #4 math: `docs/research/frontier-math.md` — linear-space dominance; unsmoothed ridge sorted (cost↑, intel↑, speed↓); log-min-max speed+cost / linear min-max intel; score = Σ(wᵢ·x̂ᵢ)/Σw over visible set; fixed frontier geometry + live re-rank; 5 presets; ε-clamp for $0; nulls excluded and listed.
  - #5 Plotly: `docs/research/plotly-dechrome.md` — de-chrome native; sweep = restyle+rAF; cinema orbit = per-frame relayout; no 3D→pixel API (cursor-anchored tooltip); Plotly.animate 2D-only; 8 marker symbols; Plotly.react needs immutable updates.
  - #6 axis: TPS on x; TTFT in readout. #7 tooling: Vite + vanilla TS, no framework, typed dataset. #8 publish: Cloudflare Pages viz.kyanitelabs.tech (gated). #9 fonts: Inter Tight + IBM Plex Mono, free.
- Open: #10 first-render visual sign-off (the gate this build must clear).

## Constraints

- Planning/execution boundary: ralplan writes only `.omx/` artifacts; no code edits in this session.
- No default Plotly chrome; no smoothing on the ridge; no categorical color; no emoji; no ambient idle motion.
- Publish is approval-gated (external action); not part of v0 build execution.
- n ≈ 35 points — O(n²) frontier is fine; no perf engineering beyond sensible practice.

## Unknowns / open questions

- Exact provider→shape mapping (8 symbols vs ~14 provider strings; grouping strategy needed).
- Whether Plotly restyle+rAF sweep feels right at 400ms — resolved empirically at first render (#10).
- Filament-white vs mineral-gold — pixel sign-off at #10.
- Whether Gemini 2.5 Pro aider_pct approximate match stays (data-quality call; default: keep with note).

## Likely codebase touchpoints

New repo — everything is greenfield under `~/workspaces/llm-3d-viz`: `package.json`, `vite.config.ts`, `index.html`, `src/` (new), `data/models.v0.draft.json` (exists), `docs/research/*.md` (exists).

# Deploy status — 2026-08-08

## Live public

**https://viz.kyanitelabs.tech/**

- Pages deploy production: commit `27f2fb4` via wrangler `pages deploy dist --project-name=llm-3d-viz --branch=main --commit-dirty=true`
- Worker proxy `viz-kyanitelabs-proxy` → `llm-3d-viz.pages.dev`
- Membrane regression guard: **whitePct 1.22%** (healthy; whiteout ≈50%)

## Source pushed

| Remote | Branch | Commit | Date |
|--------|--------|--------|------|
| Forgejo `origin` | `main` | `a673e51` | 2026-08-08 |
| GitHub `oss` | `oss/public` | `7fe32d5` | 2026-08-08 |

Local `main` in sync with `origin/main` (0 ahead, 0 behind). Working tree clean.

## Verified surfaces

| Path | Status |
|------|--------|
| `/` | Pareto membrane + skirt + spine visible; whitePct 1.22% |
| `/llms.txt` | `text/plain` |
| `/about.md` | `text/markdown` |
| `/og-image.png` | `image/png` |
| `/sitemap.xml` | `application/xml` |

## Verification commands

```bash
# deterministic membrane pixel check
CAPTURE_URL="https://viz.kyanitelabs.tech/" node scripts/verify-membrane.mjs

# gates
npm test                               # 271 pass
npx tsc --noEmit                       # clean
npm run build                          # clean
```

## Note

Cloudflare **Managed** robots still prepends AI crawler Disallows on `/robots.txt` at the zone layer. Repo `public/robots.txt` is deployed; zone managed rules may still block GPTBot/ClaudeBot for training. Content discovery via llms.txt works.

## Local CLI/MCP

```bash
npm run atlas:cli -- meta
npm run atlas:mcp
npm run catalog:coverage
```

Unit tests: **271 pass**.

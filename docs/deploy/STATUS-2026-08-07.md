# Deploy status — 2026-08-07

## Live public

**https://viz.kyanitelabs.tech/**

- Pages deploy production (latest via wrangler `pages deploy dist --project-name=llm-3d-viz --branch=main`)
- Worker proxy `viz-kyanitelabs-proxy` → `llm-3d-viz.pages.dev`
- Ultra QA (Playwright, reduced-motion off): **11/11 pass** — cinema enter/exit FAB + Atlas visible, floor Apply, provenance, coverage badge, llms.txt/about.md/og-image.png content-types

## Verified surfaces

| Path | Status |
|------|--------|
| `/` | SEO title + JSON-LD + coverage method strip |
| `/llms.txt` | `text/plain` |
| `/about.md` | `text/markdown` |
| `/og-image.png` | `image/png` |
| `/sitemap.xml` | `application/xml` |

## Note

Cloudflare **Managed** robots still prepends AI crawler Disallows on `/robots.txt` at the zone layer. Repo `public/robots.txt` is deployed; zone managed rules may still block GPTBot/ClaudeBot for training. Content discovery via llms.txt works.

## Local CLI/MCP

```bash
npm run atlas:cli -- meta
npm run atlas:mcp
npm run catalog:coverage
```

Unit tests: **207 pass**.
